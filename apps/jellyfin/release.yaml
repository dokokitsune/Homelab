apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: jellyfin
  namespace: jellyfin
spec:
  interval: 30m
  chart:
    spec:
      chart: jellyfin
      version: 2.5.0
      sourceRef:
        kind: HelmRepository
        name: jellyfin
        namespace: jellyfin
      interval: 12h
  values:
    image:
      repository: ghcr.io/linuxserver/jellyfin
      tag: 10.11.2@sha256:e80d9e506715874c7741abd4f669e143c011cf3083c71c90b6df1ff4498f74d3
    service:
      type: LoadBalancer
    podSecurityContext:
      fsGroup: 3000
    securityContext:
      privileged: true
    volumes:
      - name: truenas-media
        nfs:
          server: 10.246.144.2
          path: /mnt/Bulk/media
      - name: dri
        hostPath:
          path: /dev/dri/renderD128
    volumeMounts:
      - name: truenas-media
        mountPath: /mnt/Bulk/media
      - name: dri
        mountPath: /dev/dri/renderD128
    persistence:
      config:
        storageClass: iscsi
        size: 10Gi
      media:
        enabled: false
    metrics:
      enabled: true
      serviceMonitor:
        enabled: true
        namespace: monitoring
    resources:
      limits:
        gpu.intel.com/i915: 1
